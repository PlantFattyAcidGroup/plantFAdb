<% content_for :top_bar do %>
  <h1 class="pagetitle">Create Dataset</h1>
<% end %>


<% content_for :item_bar do %>
  <%= link_to 'Cancel', edit_plants_pub_path(@dataset.plants_pub_id), class: 'btn btn-primary' if @dataset.plants_pub_id%>
<% end %>

<%= render partial: 'form', locals: {dataset: @dataset} %>