<%= form_for(@systematic_name) do |f| %>
  <% if @systematic_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@systematic_name.errors.count, "error") %> prohibited this systematic_name from being saved:</h2>

      <ul>
      <% @systematic_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
