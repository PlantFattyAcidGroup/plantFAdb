<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag('PhyloFAdb_version2-07.png'), root_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
				<%= application_top_navbar_items %>
				<li><%= link_to "Downloads", Page.path('downloads') %></li>
				<% if user_signed_in? && current_user.is_admin? %>
				<li class="dropdown <%= 'active' if params[:controller] =~ /users/ %>">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
						Admin <span class="caret"></span>
					</a>
          <ul class="dropdown-menu">
						<li><%= link_to "Manage Users", users_path %></li>
						<li><%= link_to "Page Content", pages_path %></li>
          </ul>
				</li>
				<% end %>
      </ul>
			<ul class="nav navbar-nav navbar-right">
				<% if user_signed_in? %>
					<li class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
							<%= current_user.email %> <span class="caret"></span>
						</a>
	          <ul class="dropdown-menu">
	            <li class='dropdown-header'>Signed in as <%= current_user.email %></li>
							<li role="separator" class="divider"></li>
							<li><%= link_to "My Account", current_user %></li>
							<li role="separator" class="divider"></li>
	            <li><%= link_to "Help", Page.path('help') %></li>
							<li><%= link_to "About", Page.path('about') %></li>
							<li><%= link_to "Contact", Page.path('contact') %></li>
	            <li role="separator" class="divider"></li>
	            <li><%= link_to "Log Out", destroy_user_session_url, method: :delete %></li>
	          </ul>
					</li>
				<% else %>
				
					<li><%= link_to "Sign In", new_user_session_path, class: "navbar-link" %></li>

				<% end %>
			</ul>
    </div>
  </div>
</nav>
