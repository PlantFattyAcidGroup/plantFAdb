<%= form_for(@parameter) do |f| %>
  <% if @parameter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parameter.errors.count, "error") %> prohibited this parameter from being saved:</h2>

      <ul>
      <% @parameter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :delta_notation %><br>
    <%= f.text_field :delta_notation %>
  </div>
  <div class="field">
    <%= f.label :cas_number %><br>
    <%= f.text_field :cas_number %>
  </div>
  <div class="field">
    <%= f.label :sofa_mol_id %><br>
    <%= f.text_field :sofa_mol_id %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
